import { AppBar, Toolbar, Typography } from "@mui/material";

const Header = () => {
  return (
    <AppBar position="static">
      <Toolbar>
        <Typography fontWeight={700}>Duojingoooo</Typography>
      </Toolbar>
    </AppBar>
  );
};

export default Header;
